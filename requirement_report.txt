Bạn là một kỹ sư chuyên sâu về Spark/MapReduce kiêm technical writer học thuật. 
Nhiệm vụ của bạn là đọc toàn bộ code Scala đã được viết sẵn và soạn thảo một 
báo cáo LaTeX hoàn chỉnh, có chiều sâu học thuật cho bài lab Big Data Analysis.

=== BỐI CẢNH ===
Báo cáo bao gồm 4 task xử lý dữ liệu phân tán trên bộ dữ liệu Amazon Sale Report
sử dụng Apache Spark và MapReduce, ngôn ngữ Scala.

────────────────────────────────────────────────────────────
BƯỚC 1 — ĐỌC VÀ PHÂN TÍCH TOÀN BỘ FILE SCALA
────────────────────────────────────────────────────────────
Trước khi viết bất cứ điều gì, hãy đọc TẤT CẢ các file Scala trong 4 thư mục:
  - src/Task_1-1/source/*.scala   (MapReduce sliding window)
  - src/Task_1-2/source/*.scala   (MapReduce median variety)
  - src/Task_2-1/source/*.scala   (Spark DataFrame cancelled orders)
  - src/Task_2-2/source/*.scala   (Spark DataFrame stddev + percentile)

Với mỗi file, hãy rút ra:
  1. Luồng xử lý dữ liệu tổng thể và thuật toán chính được dùng
  2. Các transformation, groupBy, join, window function quan trọng
  3. Cách tổ chức Map phase / Reduce phase / Combiner (Task 1-x)
  4. Cách chaining các DataFrame/Dataset API (Task 2-x)
  5. Mọi logic tùy chỉnh (ví dụ: exact percentile tự cài ở Task 2-2)
  6. Comments và tên biến thể hiện quyết định thiết kế của tác giả

────────────────────────────────────────────────────────────
BƯỚC 2 — CẤU TRÚC BÁO CÁO
────────────────────────────────────────────────────────────
Báo cáo gồm các phần sau. Mỗi task là một \section{} riêng biệt.

[PHẦN MỞ ĐẦU]
  - Giới thiệu bộ dữ liệu Amazon Sale Report (schema, kích thước, đặc điểm)
  - Môi trường thực thi (Spark version, Scala version, cluster/local mode)
  - Lý do chọn Scala (điểm ngôn ngữ đầy đủ, type-safety, Dataset API)
  - Tổng quan cấu trúc báo cáo

[VỚI MỖI TASK — viết 4 subsection sau]

  2.1 Phân tích đề bài
      - Diễn giải lại bài toán bằng ngôn ngữ chính xác, học thuật
      - Định nghĩa hình thức mọi khái niệm quan trọng, ví dụ:
          * Task 1-1: "bought", sliding window, unseen timestamp
          * Task 1-2: "variety", "qualifying style", median
          * Task 2-1: "temporally-valid promotion", "active period", percentage
          * Task 2-2: promotion count, percentile threshold, population stddev
      - Xác định tất cả edge case trong đề và cách xử lý trong code
        (VD: window ngắn hơn 7 ngày, nhóm < 2 orders, promotion chỉ xuất hiện 1 ngày)

  2.2 Phân rã thành các bước nguyên tố
      - Liệt kê theo số thứ tự các bước tính toán cơ bản
      - Mỗi bước phải có lý giải: TẠI SAO cần bước này, TẠI SAO ở vị trí này
      - Dùng ký hiệu hình thức khi phù hợp, ví dụ:
          GROUP BY (state, date), FILTER Qty > 0 ∧ status LIKE "%shipped%"
      - Trình bày dưới dạng pseudocode trong môi trường \begin{algorithm}

  2.3 Chiến lược cài đặt
      - Ánh xạ từng bước đã phân rã sang code Scala/Spark thực tế
      - Task 1-x: giải thích input/output key-value của Map phase,
        logic của Reduce phase, và nếu có Combiner thì tại sao dùng được
      - Task 2-x: giải thích chuỗi transformation DataFrame với lý do
        cho từng lựa chọn (VD: tại sao dùng window function thay vì join,
        tại sao dùng explode thay vì UDF cho promotion-ids)
      - Chèn code snippet minh họa (KHÔNG copy toàn bộ file, chỉ lấy
        phần quan trọng nhất) trong \begin{lstlisting}[language=Scala]

  2.4 Phân tích chuyên sâu (theo từng task)

      ► Task 1-1 riêng:
          - Tại sao naive nested-loop trên tất cả cặp ngày là bất khả thi
          - Key design trong MapReduce giải quyết vấn đề này thế nào
          - Cách xử lý "unseen timestamps" trong output

      ► Task 1-2 riêng:
          - Tại sao median KHÔNG thể tính bằng Combiner đơn giản
          - Chiến lược tính median trong MapReduce context (two-pass, sorting, v.v.)
          - Cách xác định "size ≥ XXL" — parsing logic và thứ tự so sánh

      ► Task 2-1 riêng (BẮT BUỘC):
          - Dán output của explain(true) vào block \begin{verbatim}
            [ĐÁNH DẤU RÕ: placeholder để paste kết quả thực tế vào đây]
          - Phân tích physical join strategy Spark đã chọn:
              * Loại join: BroadcastHashJoin / SortMergeJoin / BroadcastNestedLoopJoin
              * Tại sao Spark chọn strategy đó (kích thước bảng, broadcast threshold)
              * DataFrame nào là "build side" trong BroadcastHashJoin và tại sao
          - Đếm và giải thích từng Exchange node (shuffle) trong plan
          - Xác định số stages và điều gì tạo ra ranh giới giữa các stage
          - Thảo luận vai trò của AQE (Adaptive Query Execution) nếu có

      ► Task 2-2 riêng (BẮT BUỘC):
          - Giải thích approx_percentile dùng thuật toán xấp xỉ nào bên trong
            (Greenwald-Khanna hoặc t-digest) và accuracy guarantee của nó
          - Exact percentile tự cài: giải thích cách sort + row_number
            để xác định vị trí percentile, độ phức tạp O(n log n)
          - Bảng so sánh accuracy: threshold exact vs approximate cho
            một số SKU-month groups mẫu, tính absolute difference
            [ĐÁNH DẤU: placeholder — điền sau khi chạy benchmark]
          - Bảng benchmark thời gian thực thi: ≥ 5 lần chạy, 
            trình bày mean ± stddev cho cả 2 approaches
            [ĐÁNH DẤU: placeholder — điền sau khi chạy benchmark]
          - Phân tích các SKU-month groups mà 2 approaches cho
            qualifying orders KHÁC NHAU — tại sao xảy ra (lỗi xấp xỉ)
          - Nếu có group > 1000 orders: thảo luận repartitioning,
            lý do chọn partition strategy, so sánh với default 128MB

[PHẦN KẾT LUẬN]
  - Tổng kết trade-off giữa MapReduce và Spark Structured APIs
  - So sánh ease of implementation vs performance
  - Bài học rút ra từ 4 task

────────────────────────────────────────────────────────────
BƯỚC 3 — YÊU CẦU ĐỊNH DẠNG LATEX
────────────────────────────────────────────────────────────
Preamble phải dùng:
  \documentclass[12pt,a4paper]{article}
  Packages bắt buộc: geometry, listings, xcolor, booktabs, graphicx,
  amsmath, hyperref, fancyhdr, titlesec, caption, float,
  multirow, algorithm, algpseudocode, subcaption

Định dạng cụ thể:
  - Code Scala: \begin{lstlisting}[language=Scala] với syntax highlighting
    tự định nghĩa trong preamble (keyword màu xanh, string màu đỏ, v.v.)
  - Tất cả bảng: dùng booktabs (\toprule \midrule \bottomrule),
    bọc trong \begin{table}[H] với caption và label
  - Công thức toán học trong môi trường equation hoặc \[ \]:
      * Population stddev: σ = \sqrt{\frac{1}{N}\sum_{i=1}^{N}(x_i - \mu)^2}
      * Median (n lẻ / n chẵn)
      * Sliding window range: [d-7, d-1]
  - Pseudocode: dùng \begin{algorithm} + \begin{algorithmic}
    cho ít nhất Task 1-1 và Task 1-2
  - Header/footer: fancyhdr với tên môn học, tên task, số trang
  - Hyperref: tất cả \ref và \cite đều clickable

────────────────────────────────────────────────────────────
BƯỚC 4 — TIÊU CHUẨN CHIỀU SÂU
────────────────────────────────────────────────────────────
KHÔNG viết mô tả bề mặt. Với MỌI quyết định thiết kế, phải trả lời:
  "Tại sao làm vậy thay vì cách khác?"

Ví dụ các câu hỏi cần được trả lời trong báo cáo:
  - Task 1-1: Tại sao emit key = (state, date_in_window) thay vì (state, current_date)?
  - Task 1-2: Tại sao không dùng TreeMap/SortedMap trong Reducer để tính median?
  - Task 2-1: Tại sao join promotion validity table trước hay sau khi filter?
              Thứ tự join có ảnh hưởng đến shuffle cost không?
  - Task 2-2: Khi nào approx_percentile cho kết quả đủ tốt và khi nào
              exact là bắt buộc? Ảnh hưởng đến correctness của stddev ra sao?

Mọi nhận định phải được neo vào hành vi thực tế của code,
KHÔNG phát biểu chung chung.

────────────────────────────────────────────────────────────
OUTPUT
────────────────────────────────────────────────────────────
Tạo ra một file .tex duy nhất, hoàn chỉnh, compile được bằng pdflatex.

Placeholder chấp nhận được (đánh dấu rõ ràng) CHỈ cho:
  - Output của explain(true)  → ghi: % [PASTE explain(true) OUTPUT HERE]
  - Số liệu benchmark         → ghi: % [PASTE BENCHMARK RESULTS HERE]

Tất cả phần còn lại phải được viết đầy đủ dựa trên phân tích code thực tế.
Không được để trống hay dùng placeholder cho phần phân tích.